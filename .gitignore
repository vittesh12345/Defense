# Python
__pycache__/
*.pyc
.venv/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Model weights (downloaded, never committed)
*.pt
*.onnx

# Generated outputs — CLI writes report/results JSONs to the repo root.
# (No committed JSON lives at the repo root; baselines/fixtures are in subdirs.)
/*.json
build/

# macOS
.DS_Store
